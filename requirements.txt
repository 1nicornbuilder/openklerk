# OpenClerc — Engine dependencies only (no SaaS deps)
playwright>=1.41.0
click>=8.1.0
rich>=13.7.0
pydantic>=2.6.0
google-cloud-aiplatform>=1.40.0
httpx>=0.26.0
Pillow>=10.0.0
thefuzz>=0.22.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
